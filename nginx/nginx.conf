server {
    listen 80;
    server_name workers-project.onrender.com localhost;

    location / {
        proxy_pass http://app:3000;
    }

    location /health {
        proxy_pass http://app:3000/health;
        access_log off;
    }
}